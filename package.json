{
  "name": "cron-expression-generator",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=cron-expression-generator"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.4",
    "croner": "^10.0.1",
    "cronstrue": "^3.24.0",
    "tailwindcss": "^4.3.2"
  }
}